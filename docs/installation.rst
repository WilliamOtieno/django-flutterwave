============
Installation
============

At the command line::

    $ easy_install django-flutterwave

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-flutterwave
    $ pip install django-flutterwave
